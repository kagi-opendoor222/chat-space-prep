.sidebar
  .top
    .top__name
      %h2
        = current_user.name
    %ul.top__list
      %li
        %a{:href => "#"}
          = fa_icon "edit"
      %li
        / %a{:href => "#"}
        /   = fa_icon "cog"
        = link_to edit_user_path(current_user) do
          = fa_icon "cog"
  .chatgroups
    .chatgroup
      %p.chatgroup__group-name
        groupname
      %p.chatgroup__latest-message
        まだメッセージはありません。
    .chatgroup
      %p.chatgroup__group-name
        groupname
      %p.chatgroup__latest-message
        まだメッセージはありません。
