.sidebar
  .top
    .top__name
      %h2
        = current_user.name
    %ul.top__list
      %li
        = link_to new_group_path do
          = fa_icon "edit"
      %li
        = link_to edit_user_path(current_user) do
          = fa_icon "cog"
  .chatgroups
    - current_user.groups.each do |group|
      = link_to group_messages_path(group), class: "group-link" do
        .chatgroup
          .chatgroup__name
            = group.name
          .chatgroup__message
            -if group.messages.last && group.messages.last.content.present?
              = group.messages.last.content
            -elsif group.messages.last && group.messages.last.image
              画像が表示されています。
            -else
              メッセージが投稿されていません。
